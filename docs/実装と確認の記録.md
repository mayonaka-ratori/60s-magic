# 第一段階の実装と確認

確認日：2026年9月20日。対象は最初の24秒だけです。

## 実施した確認

| 確認 | 結果 |
| --- | --- |
| 入力時刻、元の線の保持、魔法の決定、Jevへの要求、音の取り込み、ローカル認識との接続、詠唱辞書、術式の配置、命中位置、騎士の反応と音の時刻 | 57項目すべて通過 |
| 実際のブラウザーで24秒を通して遊ぶ | 通過。途中の描き足し、11秒以降の最後の線、七つの雷、感想の記録を確認 |
| Jevの返事が3秒遅れる場合 | 通過。返事を使わず氷の壁で予定どおり発動 |
| カメラの認識処理を別の場所で起動し、中止する | 通過。ブラウザーが生成した映像を使用 |
| 見本の再生の明示と中止 | 通過 |
| 新しい背景での完成・放出・命中・結果表示 | 通過。14秒まで線の位置を変えず、17秒に発動。結果では戦闘中の線を消し、枠内に本人の術式を表示 |
| 見本との比較と描き直し | 通過。横並び、二筆の入力、完成形への配置、390×844の確認画面 |
| 大きい二筆の入力と、縦画面での再プレイ | 通過。完成時の縮小、390×844の結果、再開時に背景のぼかしが解除されることを確認 |
| 騎士の反応と効果音 | 通過。18.5秒でひるみ、構えを戻す。試聴、実際の音の出力、消音、中止後の無音、再プレイを確認 |
| 騎士を立体のモデルへ置き換え | 開発用の環境で通過。待機・被弾・構えを戻すの姿勢、背景の一枚絵との位置合わせ、命中位置が胸に付いてくること、390×844の表示を画像で確認 |
| 効果音とローカル認識の同時動作 | 通過。録音中は効果音を鳴らさず、14.75秒以降の音と七つの雷の発動を確認 |
| Jevの期限内の回答 | 試験用の回答で通過。曖昧な言葉が氷の壁になり、17秒より前には発動しない |
| 配布用の画面の生成 | 通過。上のブラウザー5項目はこの画面でも確認 |
| 日本語表示 | 1440×900の開始・描画・完成・発動・終了画面を画像で確認。見出しの不自然な折り返しを修正 |
| 依存するソフトの既知の問題の検査 | `npm audit` で報告0件 |
| PC内で日本語の合成音声を認識 | 4種類を各3回、計12回で魔法の種類や指示が一致。無音では文字を返さないことも確認 |
| PC内の認識をゲームのマイク入力から使用 | 通過。終了直前の声を取り込み、最後の線と合わせて七つの雷が発動 |
| 詠唱辞書の表示と、かなで入力した難語からの発動 | 通過。読み方の一覧、元の文字と変換後の保存、七つの雷を確認 |
| 難語を含む14種類の合成音声 | 7種類で指定語と魔法の指示が一致。聞き違いが残るため、全語の認識成功とはしない。[詳しい記録](詠唱辞書と認識の確認.md) |

ローカル音声認識はKotoba-Whisper v2.0、faster-whisper 1.2.1、CTranslate2 4.8.2を使用しています。認識用ファイルは版 `f44edd35eaeb2274e85ac7b31fb2c6f59ff1c4bc` に固定しました。RTX 4070 SUPER 12GBで、読み込み後の合成音声一回分の認識は約110〜125msでした。これは話してから表示されるまでの時間ではありません。

合成音声では「分かれろ」が「ふんかれろ」、「氷よ」が「氷を」になる例があり、文字の完全一致ではありません。雷・七つ・氷・壁・攻撃しない指示が魔法に反映されることを確認しています。実際の人の声で同じ精度になることは未確認です。結果は `.local-speech/test-report.json` と `.local-speech/browser-test-report.json` に置き、Gitには含めません。

ブラウザーはChrome for Testing、描画はこのPCのDirect3D 11で確認しました。最初の自動確認で使ったソフトウェア描画では操作の送信が遅かったため、実機の画像処理を使う設定へ変更しています。実機の手の追跡が目標性能を満たしたという意味ではありません。

新しい術式の描画を本編へ接続した後、ローカル音声認識とマウスを使った24秒を再確認しました。一回の測定は平均59.75fps、フレーム間隔の99パーセンタイル16.9ms、発動17.009秒、最後の認識処理116.8msでした。合成音声を使用し、カメラは同時使用していません。人の手と声での精度・遅れの確認は別に残します。

その後、画像で動かす騎士と効果音を追加した回は平均56.63fps、フレーム間隔の99パーセンタイル18.5ms、発動17.010秒でした。こちらも合成音声とマウスです。[騎士と音の詳しい確認](騎士の動きと効果音.md)。

騎士を立体のモデルへ置き換えた回は、GPUのない開発用の環境で作業したため、速度を測り直していません。この環境はソフトウェア描画で毎秒1.6画面しか出ず、画像の騎士でも毎秒1.7画面でした。重くなった分はわずかですが、実機の数値ではありません。画面が遅いと0.62秒しかないひるみの姿勢を飛ばすため、`npm run test:browser` の被弾の確認もGPUのあるPCで行う必要があります。24秒を通して遊ぶブラウザーの確認も、この環境では操作が間に合わず終わってしまいます。置き換える前のコードでも同じところで止まったため、今回の変更による違いではありません。確認画面の3姿勢は時刻を止めて見るため、この環境でも通りました。

## まだ確認していないこと

- Jevとの実通信。接続情報が未設定です。要求の形式、回答の読み取り、期限、回答の採用は試験用の返事で確認しました。
- Google Cloudとの実通信。接続情報が未設定です。マイクの44.1kHz・48kHzから16kHzへの変換、短い無音の扱い、14秒での録音終了は生成した音で確認しました。
- 実際の人の手の認識精度と、動いてから線が出るまでの遅れ。
- 立体の騎士を入れた状態での表示速度。GPUのあるPCでの測り直しが必要です。
- 実際の人の詠唱と描画を同時に受け付けたときの認識精度、待ち時間、表示速度。
- 初見の5人による「自分の魔法を放った」という実感の確認。

次に人へ試してもらうときは [最初の24秒の確認手順](最初の24秒_確認手順.md) を使います。外部サービスの接続前でも、マウスと声で試せます。音声認識を使わず文字を入れることもできます。

## 次の段階に残したもの

24〜60秒の防御と最後の魔法、前の術式の引き継ぎ、音楽、敵の攻撃・弱点・倒れる動き、鎧や床へ届く照明、魔導書とQR、公開用の保存、会場の機器トラブル対応、連続運転の確認。最初の24秒の騎士は立体のモデルへ置き換え、待機・ひるみ・構えを戻す動きと効果音を入れました。
