# CLAUDE.md

このファイルは、Claude Code がこのリポジトリで作業するときの決まりです。

## いちばん大事な決まり：ユーザーへの話し方

ユーザーはエンジニアではありません。ユーザーへの返事は、必ず次を守ってください。

- 自然な日本語で、短く話す。結論を先に言う。
- 日常の言葉だけを使う。専門用語、英語の略語、業界の言い回しは避ける。どうしても必要なら一言で説明を添える。
- 比喩、詩的な表現、凝った言い回し、抽象的なラベル（「〜の軸」「〜の梯子」など）は使わない。
- エムダッシュ（—）は使わない。日本語の文の中に半角スペースを入れない。
- 英語を直訳したような言い方をしない。悪い例と良い例：
  - 「回２」→「２回目」
  - 「指摘の通りで、これは正直に認めます」→「ご指摘の通りです」
  - 「〜することができます」→「〜できます」
  - 「重要なのは〜ということです」→「〜が大事です」
  - 「〜しましょう」の連発→普通の説明文で書く
  - 「読者体験の向上を実現」のような名詞の積み上げ→「読者が読みやすくなる」のように動詞で書く
  - 「節」「セクション」→「見出しひとつ分」「この部分」
- ファイル名やコマンドは、ユーザーが自分で触る必要があるときだけ出す。それ以外は言葉で説明する。
- 確認してほしいことがあるときは、何をどこで見ればよいかをURLや手順つきで書く。
- できたこと、できなかったこと、まだ確認していないことをはっきり分けて伝える。

このリストは今後追加されることがあります。追加されたら同じように守ってください。

セリフや演出の文章を書くときだけは、凝った言い回しを使ってかまいません。

生成物（docxなど）は、英語でと言われない限り必ず日本語フォントにしてください。中国語の字体が混じりやすいので、必ず見直してください。

## プロジェクトの概要

「60秒魔法ゲーム」の試作です。プレイヤーが手で線を描き、声で唱えた言葉から魔法を作り、敵の騎士へ放ちます。
今は最初の24秒（一回目の魔法）だけを作っています。仕様の全体は `60秒魔法ゲーム_設計仕様.md`、作った範囲と使い方は `README.md`、確認の記録は `docs/` にあります。

主な構成：

- `src/game/`：時計、手の軌跡、発話、魔法の決定
- `src/input/`、`public/*worker*.js`、`public/audio-worklet.js`：カメラとマイク
- `src/render/`：Babylon.jsの背景、騎士、術式と魔法の描画
- `server/`：接続情報を持つサーバー、Jevへの12問、PC内とGoogleの音声認識
- `speech/`：PC内で動かす音声認識（Kotoba-Whisper）
- `tests/`：単体の試験と、Playwrightによるブラウザーの試験

## よく使うコマンド

```
npm ci                 # 依存するソフトを入れる
npm run setup:assets   # 手の認識に使うファイルを取得する
npm run setup:speech   # PC内の音声認識を準備する（uvが必要。NVIDIA GPUがなければCPUで動く）
npm run dev            # 開発用に起動する（http://localhost:5173）
npm test               # 単体の試験
npm run build          # 型の確認とビルド
npm run test:browser   # ブラウザーの試験（先に npx playwright install chromium）
```

## 作業するときの決まり

- 文章、コメント、コミットメッセージは日本語で書く。
- 秘密の値（APIキーなど）は `.env` にだけ置き、コードやチャットに貼らない。`.env.example` が見本。
- 試験を通ったことと、実機や人で確認できたことは分けて記録する。
- 仕様書にある時間の決まり（14秒で受付終了、16秒で魔法の内容を固定、17秒で発動など）を勝手に変えない。
- 外部サービスからの返事が遅れても演出を延ばさない。自動の再試行はしない。
