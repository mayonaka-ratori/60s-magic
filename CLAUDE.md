# CLAUDE.md

Claude Code がこのリポジトリで作業するときの決まりです。文書は最初にまとめて読まず、下の「このプロジェクト」の表から、その作業に要るものだけを読んでください。

## いちばん大事な決まり：ユーザーへの話し方

ユーザーはエンジニアではありません。ユーザーへの返事は、必ず次を守ってください。

- 自然な日本語で、短く話す。結論を先に言う。
- 日常の言葉だけを使う。専門用語、英語の略語、業界の言い回しは避ける。どうしても必要なら一言で説明を添える。
- 比喩、詩的な表現、凝った言い回し、抽象的なラベル（「〜の軸」「〜の梯子」など）は使わない。
- エムダッシュ（—）は使わない。日本語の文の中に半角スペースを入れない。
- 英語を直訳したような言い方をしない。悪い例と良い例：
  - 「回２」→「２回目」
  - 「指摘の通りで、これは正直に認めます」→「ご指摘の通りです」
  - 「〜することができます」→「〜できます」
  - 「重要なのは〜ということです」→「〜が大事です」
  - 「〜しましょう」の連発→普通の説明文で書く
  - 「読者体験の向上を実現」のような名詞の積み上げ→「読者が読みやすくなる」のように動詞で書く
  - 「節」「セクション」→「見出しひとつ分」「この部分」
- ファイル名やコマンドは、ユーザーが自分で触る必要があるときだけ出す。それ以外は言葉で説明する。
- 確認してほしいことがあるときは、何をどこで見ればよいかをURLや手順つきで書く。
- できたこと、できなかったこと、まだ確認していないことをはっきり分けて伝える。

このリストは今後追加されることがあります。追加されたら同じように守ってください。

セリフや演出の文章を書くときだけは、凝った言い回しを使ってかまいません。

生成物（docxなど）は、英語でと言われない限り必ず日本語フォントにしてください。中国語の字体が混じりやすいので、必ず見直してください。

## このプロジェクト

「90秒魔法ゲーム」の試作です。手で線を描き、声で唱えた言葉から魔法を作り、騎士へ放ちます。標準は「順番に」（声だけ、手だけ、手のあと声）で、「同時に」も選べます。90秒と結果画面（魔導書）まで作ってあります。QRの先の公開ページはこれからです。

| 知りたいこと | 読む場所 |
| --- | --- |
| 起動、遊び方、試験、設定、フォルダー、主な数値の置き場所 | `README.md` |
| 今どこまで確認できていて、何が未確認か | `docs/実装と確認の記録.md` |
| 文書の一覧と段階ごとの進み具合 | `docs/README.md` |
| 速さを見積もる基準のPC（Apple M4のMac） | `docs/確認に使うPC.md` |
| ゲーム全体の設計 | `90秒魔法ゲーム_設計仕様.md` |
| 防御の回ととどめの回の設計、今どこまで作ったか | `docs/防御ととどめ_画面と流れの設計.md` |

## 作業するときの決まり

- 変更したら `npm test` と `npm run build` を通す。ブラウザーと音声の試験はユーザーのMacで走らせるので、コマンドを渡して結果を見る。
- 文章、コメント、コミットメッセージは日本語で書く。既存の文書と同じ調子に合わせる。
- 秘密の値（APIキーなど）は `.env` にだけ置き、コードやチャットに貼らない。`.env.example` が見本。
- `.env`、`.venv-speech`、`.local-speech`、`public/vision/` はGitに含めない。
- 声とカメラの映像はファイルに保存しない。音声認識はこのPCの中で完結させる（Googleは `SPEECH_PROVIDER=google` のときだけ）。この方針を変える変更は入れない。
- 仕様書にある時間の決まり（二つの遊び方それぞれの受付、確定、発動など）を勝手に変えない。時刻はすべて `src/game/rounds.ts` の表から作る。コードにも文書にも秒数を書き散らさない。
- 外部サービスからの返事が遅れても演出を延ばさない。自動の再試行はしない。
- 試験を通ったことと、実機や人で確認できたことは分けて記録する。確認していないことを、確認したように書かない。
- 新しい文書を足したら `docs/README.md` の一覧にも行を足す。文書は「今の状態」が分かる形に保ち、作業の経過は短くまとめる。
